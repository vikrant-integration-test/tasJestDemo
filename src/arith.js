const add = (a, b) => a + b;
const mul = (a, b) => a * b;
const sub = (a, b) => a - b;
const div = (a, f) => a / f;
const mod = (a, f) => a % f;

module.exports = { add, mul, sub, div, mod };

//Updating test as new contributor in newBranch
