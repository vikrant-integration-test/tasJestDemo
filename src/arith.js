const add = (a, b) => a + b;
const mul = (a, b) => a * b;
const sub = (a, b) => a - b;
const div = (a, b) => a / b;
const mod = (a, b) => a % b;
const modu = (a, b) => a % b;

module.exports = { add, mul, sub, div, mod, modu };
