const comparisionOperation = {
    equal: function (a, b) {
        return a == b;
    },

    greater: function (a, b) {
        return a > b;
    },

    less: function (a, b) {
        return a < b;
    }
}

module.exports = comparisionOperation